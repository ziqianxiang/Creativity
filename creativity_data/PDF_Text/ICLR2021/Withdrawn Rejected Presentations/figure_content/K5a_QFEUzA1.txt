Figure 1: Translation floWs of lossfunction L in Eq. 1. Variable set (θα, θβ)is replaced With (θ1, θ2) and (θ2, θ1)iteratively in Algorithm 1. Parallel pairs(xs, yt),(zs, yt),(ys, xt) and (ys, zt) areused to train the final model θ.
