Table 1: Test and LOO accuracies for models pre-trained on ImageNet and transferred to CIFAR10by re-training the top layer. We use 5 runs, each with a different training set of size n = 10000. Wecompare the pre-trained networks with random networks to show the benefits of transfer learning.
Table 2: Test and LOO losses for models pre-trained on ImageNet and transferred to CIFAR10 byre-training the top layer. We use 5 runs, each with a different training set of size n = 10000. Wecompare the pre-trained networks with random networks to illustrate the benefits of transfer learning.
