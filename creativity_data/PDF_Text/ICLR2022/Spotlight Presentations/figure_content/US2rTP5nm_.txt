Figure 1: Example prediction by EntQA taken from AIDA-A. Given a passage, the retriever mod-ule ranks K candidate entities, then the reader module finds mentions of each entity or rejects it(marked by X). Both modules use entity descriptions (not shown). In this example, it predicts thespan “England” for the 11th candidate England cricket team but rejects the 35th candidateEngland (the country).
