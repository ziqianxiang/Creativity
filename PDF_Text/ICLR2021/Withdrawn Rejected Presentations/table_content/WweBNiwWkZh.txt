Table 1: Dataset generation analysis (in cm). To measure variance in d(θ), we calculate the change ind(θ) between any two vertices that share an edge in the triangle mesh, denoted by ∆d(θ).
Table 3: Test dataset, average volume errors (cm3).
