Table 1: Test set error (%) for vanilla SGD (baseline), eSGD and rSGD. The first threecolumns show results obtained with the same number of passes over the training data. In thelast column instead, each replica in the parallelizable rSGD algorithm consumes the sameamount of data as the baseline.
