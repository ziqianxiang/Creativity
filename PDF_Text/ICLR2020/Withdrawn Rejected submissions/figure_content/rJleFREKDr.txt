Figure 1: Architecture of our approach based on a neural encoder that generates input representationsand a trainable controller that learns to store and maintain them in an external memory formed bykey-value pairs. The memory is modified via operations a, each of which returns a reward signal r.
