Table 1: Cross performance between the DWE embedding learned on each datasets. On each row, weobserve the MSE (table a) and relative MSE (table b) on the test set of each dataset given a DWL(Cat, Crab, Faces and MNIST).
