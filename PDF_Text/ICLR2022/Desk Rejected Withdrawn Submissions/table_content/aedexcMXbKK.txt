Table 1: Result for features on CIFAR10 with ResNet18/50 under DP condition. We select 512features from ResNet50 by DFS, then we see ResNet50 performs better than ResNet18. But the testaccuracy of ResNet50 by t-statistics is less than that of ResNet18.
